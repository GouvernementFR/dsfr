const BASE = {
  id: 'base',
  name: '_base',
  module: 'base'
};

const LEGACY = {
  id: 'legacy',
  name: '_legacy',
  module: 'legacy'
};

const FILENAMES = [BASE, LEGACY];

module.exports = { FILENAMES, BASE, LEGACY };
