const MODULE = {
  id: 'module',
  filename: 'module'
};

const NOMODULE = {
  id: 'nomodule',
  filename: 'nomodule'
};

const SUPPORTS = [MODULE, NOMODULE];

module.exports = { SUPPORTS, MODULE, NOMODULE };
