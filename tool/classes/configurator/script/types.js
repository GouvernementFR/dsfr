const MODULE = {
  id: 'module',
  filename: 'module'
};

const NOMODULE = {
  id: 'nomodule',
  filename: 'nomodule'
};

const TYPES = [MODULE, NOMODULE];

module.exports = { TYPES, MODULE, NOMODULE };
