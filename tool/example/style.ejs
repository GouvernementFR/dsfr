<%
for (const file of Object.keys(files.style)) {

  const dependencies = files.style[file];
  for (const dependency of dependencies) {
%>
  <link href="<%= relativeRoot %><%= dependency %>" rel="stylesheet">
<% }
} %>
