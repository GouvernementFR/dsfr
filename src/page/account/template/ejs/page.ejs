<%
 const page = locals.page || {};
 const classes = page.classes || [];
 const attributes = {
   ...page.attributes || {},
   role: 'main',
   id: 'content'
 };
 %>

<main <%- includeClasses(classes); %> <%- includeAttrs(attributes); %>>
  <% if (page.heading) { %>
  <%- include('elements/heading', { heading: page.heading }); %>
  <% } %>

  <% if (page.block) { %>
    <%- include(`elements/blocks/${page.block.type}`, { block: page.block }); %>
  <% } %>
</main>
