<%
const sample = getSample(include);
const recover = {
  segments: [
    {
      type: 'alert',
      data: {
        text: getText('confirmation.alert', 'recover'),
        type: 'success',
        hint: getText('confirmation.alert.hint', 'recover')
      }
    }
  ]
};
%>

<%- sample('', '../sample/default.ejs', { recover: recover }, true, '../../../../example/layout', {}); %>
