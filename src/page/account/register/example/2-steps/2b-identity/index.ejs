<%
const sample = getSample(include);
const form = JSON.parse(include('../../data/identity.json.ejs', {}));

const register = {
  segments:
  [
    {
      type: 'stepper',
      data: {
        title: getText('steps.identity', 'register'),
        nextStep: getText('steps.address', 'register'),
        currentStep: 2,
        stepCount: 3
      }
    },
    {
      type:'hr'
    },
    {
      type: 'form',
      data: form
    }
  ]
}
%>

<%- sample('', '../../../example/sample/default.ejs', { register: register }, true, '../../../../../example/layout', {}); %>
