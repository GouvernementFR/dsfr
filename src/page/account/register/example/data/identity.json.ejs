<%
const sexData = JSON.parse(include('../../../../../pattern/civility/example/data/sex.json.ejs'));
const nameData = JSON.parse(include('../../../../../pattern/name/example/data/name.json.ejs', { name: { fields: ['lastname', 'firstname'], checkbox: true }}));

const data = {
  id: 'identity',
  legend: `<h6>${getText('identity.legend', 'register')}</h6>`,
  classes: [prefix + '-mb-0'],
  elements: [
    {
      type: 'hint',
      data: {
        text: getText('hint.mandatory', 'form')
      }
    },
    {
      type: 'fieldset',
      data: sexData
    },
    {
      type: 'fieldset',
      data: nameData
    },
    {
      type: 'buttonsGroup',
      base: 'button',
      template: 'buttons-group',
      data: {
        inline: true,
        classes: [prefix + '-btns-group--right', prefix + '-mt-2v'],
        buttons: [
          {
            label: getText('button.previous', 'register'),
            kind: 2
          },
          {
            label: getText('button.valid', 'register'),
            kind: 1,
            type: 'submit'
          }
        ]
      }
    }
  ]
};
 %>

<%- JSON.stringify(data); %>
