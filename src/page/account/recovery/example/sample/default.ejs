<%
	const sample = getSample(include);
	const recovery = locals.recovery || {};

	const create = recovery.create || {};

	const data = {
		title: getText('label.default', 'recovery'),
		chapo: getText('chapo.default', 'recovery'),
		create: create,
	};
 %>

<%- include('../../../template/ejs/recovery.ejs', { recovery: data }); %>
