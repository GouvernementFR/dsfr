<%
const sample = getSample(include);
const recovery = locals.recovery || {};

const page = {
  h2: {
    title: getText('label.default', 'recovery'),
    chapo: getText('chapo.default', 'recovery')
  },
  block: {
    type: 'col-6-grey-bg',
    path: `../../../../recovery/template/ejs/${recovery.template}`,
    data: { recovery: recovery }
  }
};
%>

<%- include('../../../template/ejs/page.ejs', { page: page }); %>
