<%
const sample = getSample(include);
const recovery = locals.recovery || {};

const page = {
  heading: {
    title: getText('heading.title', 'recovery'),
    chapo: getText('heading.desc', 'recovery')
  },
  block: {
    type: 'colored',
    col: 6,
    segments: recovery.segments
  }
};
%>

<%- include('../../../template/ejs/page.ejs', { page: page }); %>
