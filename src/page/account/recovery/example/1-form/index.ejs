<%
const sample = getSample(include);

const recovery = {
  segments: [
    {
      type: 'form',
      data: JSON.parse(include('../data/recovery.json.ejs'))
    }
  ]
}
%>

<%- sample('', '../../example/sample/default.ejs', { recovery: recovery }, true, '../../../../example/layout', {}); %>
