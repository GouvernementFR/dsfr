<%
const sample = getSample(include);
const recovery = {
  segments: [
    {
      type: 'alert',
      data: {
        text: getText('confirmation.alert', 'recovery'),
        type: 'success',
        hint: getText('confirmation.alert.hint', 'recovery')
      }
    }
  ]
};
%>

<%- sample('', '../sample/default.ejs', { recovery: recovery }, true, '../../../../example/layout', {}); %>
