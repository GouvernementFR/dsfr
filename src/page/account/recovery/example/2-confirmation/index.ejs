<%
const sample = getSample(include);
const recovery = {
  segments: [
    {
      type: 'alert',
      data: {
        text: getText('alert.confirmation', 'recovery'),
        type: 'success',
        hint: getText('hint.confirmation', 'recovery')
      }
    }
  ]
};
%>

<%- sample('', '../../example/sample/default.ejs', { recovery: recovery }, true, '../../../../example/layout', {}); %>
