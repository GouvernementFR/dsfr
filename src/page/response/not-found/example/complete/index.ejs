<%
const sample = getSample(include);
%>

<%- sample('', '../default/sample/default', { response: { contact: true, description: getText('page.description.full', 'not-found') } }, true, '../../../../example/layout', { breadcrumb: false, search: true }); %>
