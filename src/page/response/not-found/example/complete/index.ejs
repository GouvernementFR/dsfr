<%
const sample = getSample(include);
%>

<%- sample('', '../default/sample/default', { response: { contact: true, description: getText('page.description.full', 'not-found') } }, true, '../layout', { search: true }); %>
