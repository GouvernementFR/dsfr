<%
const sample = getSample(include);
%>

<%- sample('', '../default/sample/default', { response: { description: getText('page.description.search', 'not-found') } }, true, '../../../../example/layout', { breadcrumb: false, search: true }); %>
