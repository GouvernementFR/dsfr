<%
const sample = getSample(include);
%>

<%- sample('', '../default/sample/default', { response: { contact: true, description: getText('page.description.contact', 'not-found') } }, true, '../layout'); %>
