<%
const sample = getSample(include);
%>

<%- sample('', '../default/sample/default', { response: { contact: true, description: getText('page.description', 'unexpected') } }, true, '../../../../example/layout', {breadcrumb: false}); %>
