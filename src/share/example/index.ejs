<% const sample = getSample(include); %>

<%- sample('Méta données', './samples/meta-share', {}, true); %>
<%- sample('Boutons de partage par défaut', './samples/share-default', {}, true); %>
<%- sample('Boutons de partage version inactive', './samples/share-default', {clickable: false, group: true}, true); %>

