import ref from '../../ref.js';

import { Ratio } from './ratio/ratio.js';
import { RatioSelector } from './ratio/ratio-selector.js';

ref.artwork = {
  Ratio: Ratio,
  RatioSelector: RatioSelector
};

export default ref;
