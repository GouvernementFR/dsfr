<%
const sample = getSample(include);
const form = JSON.parse(include('../../data/address.json.ejs', {}));

const buttonsGroup = JSON.parse(include('../../data/buttons-group.json.ejs', { page: { currentStep: 3, stepCount: 3}}));

form.elements.push(buttonsGroup);

const register = {
  segments:
          [
            {
              type: 'stepper',
              data: {
                title: getText('steps.address', 'register'),
                currentStep: 3,
                stepCount: 3
              }
            },
            {
              type:'hr'
            },
            {
              type: 'form',
              data: form
            }
          ]
}
%>

<%- sample('', '../../../example/sample/default.ejs', { register: register }, true, '../../../../../example/layout', {}); %>
