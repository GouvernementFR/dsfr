<%
const sample = getSample(include);
const register = {
  segments: [
    {
      type: 'alert',
      data: {
        text: getText('confirmation.activate.alert', 'register'),
        type: 'success',
        hint: getText('confirmation.activate.alert.hint', 'register')
      }
    }
  ]
};
%>

<%- sample('', '../../sample/default.ejs', { register: register }, true, '../../../../../example/layout', {}); %>
