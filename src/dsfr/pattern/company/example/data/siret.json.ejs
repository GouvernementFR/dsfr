<%
let company = locals.company || {};
const data = {
  id: uniqueId('siret'),
  legend: {
    label: getText('fieldset.siret', 'company'),
    sr: true
  },
  elements: [
    {
      type: 'input',
      data: {
        id: uniqueId('siret'),
        label: getText('label.siret', 'company'),
        name: 'siret'
      }
    },
    {
      type: 'link',
      classes: [prefix + '-mt-n1v'],
      data: {
        label: getText('directory.label', 'company'),
        href: getText('directory.href', 'company'),
        attributes: {
          title: `${getText('directory.label', 'company')} - nouvelle fenêtre`,
          target: '_blank',
          rel: 'noopener'
        },
      }
    }
  ]
}
%>

<%- JSON.stringify(data); %>
