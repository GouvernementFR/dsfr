<%

let data = {
  label: 'Libellé action interrupteur',
  ...locals.toggle || {},
  hint: toggle.hint === true ? 'Texte d’aide pour clarifier l’action' : toggle.hint,
}

%>

<%- include('../../template/ejs/toggle-group', {toggle:data}); %>
