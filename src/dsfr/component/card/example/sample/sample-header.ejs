<% const sample = getSample(include); %>

<%- sample('Carte verticale avec taxonomie dans le header', './card-default', { card : { img: true, header: { badges: true} }}, true, './card-layout', {col: {md:4}}); %>
