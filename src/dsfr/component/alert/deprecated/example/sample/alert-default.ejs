<%
let alert = locals.alert || {};
let data = {
  ...alert,
  title: (alert.title === false) ? false : alert.title ? alert.title : 'Titre',
  text: alert.text || 'Description',
};
 %>

<%- include('../../template/ejs/alert.ejs', {alert:data}); %>
