<%
let email = locals.email || {};
const data = {
  label: email.label || getText('label', 'input-email'),
  hint: email.hint || getText('hint.format', 'input-email'),
  error: email.error ? getText('error', 'input-email') : undefined,
  autocomplete: email.autocomplete || 'email',
  attributes: {'spellcheck': false}
}
%>

<%- include('../../template/ejs/email', {email: data}); %>
