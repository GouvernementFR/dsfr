<%
const inputs = locals.inputs || {};
const input = locals.input || {};

input.valid = validData(input.valid);
input.error = errorData(input.error);
input.hint = hintData(input.hint);


const id = inputs.id;
const elements = [];
for (let i = 1; i < 4; i++) elements.push({
  path: 'input/input-base/example/sample/input-text',
  data: {
    input: {
      ...input,
      id: `${id}-${i}`,
      name: `${id}`
    }
  }
});

inputs.elements = elements;
%>

<%- include('../../../../../form/deprecated/example/sample/fieldset', {fieldset: inputs}) %>
