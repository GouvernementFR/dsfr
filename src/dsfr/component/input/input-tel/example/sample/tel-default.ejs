<%
let tel = locals.tel || {};
const data = {
  label: tel.label || getText('label', 'input-tel'),
  hint: tel.hint || getText('hint.format', 'input-tel'),
  error: tel.error ? getText('error', 'input-tel') : undefined,
  autocomplete: tel.autocomplete || 'tel',
}
%>

<%- include('../../template/ejs/tel', {tel: data}); %>
