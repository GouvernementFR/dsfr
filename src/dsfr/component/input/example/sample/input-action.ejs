<%
const input = locals.input || {}
input.action = true;
input.button = input.button || {label: getText('remove-field', 'input'), kind: 2, icon: 'delete-line', type: 'button'};
%>
<%- include('input-text', { input: input }); %>
