<%
const table = locals.table || {};
const header = table.header || {};
const footer = table.footer || {};

if (table.header) {
  table.header = include('../header/header.ejs', {header});
}

if (table.footer) {
  table.footer = include('../footer/footer.ejs', {footer});
}
%>

<%- include('../../template/ejs/table-wrapper', {table}); %>
