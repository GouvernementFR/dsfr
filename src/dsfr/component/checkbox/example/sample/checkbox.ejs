<%
let checkbox = locals.checkbox || {};
checkbox.valid = validData(checkbox.valid);
checkbox.error = errorData(checkbox.error);
checkbox.hint = hintData(checkbox.hint);

let data = {...checkbox}
data.label = checkbox.label || 'Libellé checkbox';
%>

<%- include('../../template/ejs/checkbox-group', {checkbox: data}); %>
