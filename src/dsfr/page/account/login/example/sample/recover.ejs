<%
const sample = getSample(include);
const recover = locals.recover || {};

const page = {
  heading: {
    title: getText('recover.heading.title', 'login'),
    chapo: getText('recover.heading.desc', 'login')
  },
  block: {
    type: 'colored',
    col: 6,
    segments: recover.segments
  }
};
%>

<%- include('../../../template/ejs/page.ejs', { page: page }); %>
