<%
const sample = getSample(include);
const login = locals.login || {};
const connect = JSON.parse(include('../../../../../component/connect/example/data/connect.json.ejs'));
const form = JSON.parse(include('../data/login.json.ejs', { login: login }));

const segments = [
  {
    type: 'heading',
    data: {
      title: getText('heading', 'login')
    }
  },
  {
    type: 'connect',
    data: {
      title: getText('connect.title', 'login'),
      desc: getText('connect.desc', 'register'),
      connect: connect
    }
  },
  {
    type: 'hr',
    or: true,
    data: {
      text: getText('or', 'global')
    }
  },
  {
    type: 'form',
    data: form
  },
  {
    type: 'hr'
  },
  {
    type: 'button',
    data: {
      title: getText('create.title', 'login'),
      buttonsGroup:
      {
        buttons:
        [
          {
            label: getText('create.button', 'login'),
            kind: 2
          }
        ]
      }
    }
  }
];

const page = {
  block: {
    type: 'colored',
    col: 4,
    segments: segments
  }
};

%>

<%- include('../../../template/ejs/page.ejs', { page: page }); %>
