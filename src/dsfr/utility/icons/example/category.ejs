<%
 const sample = getSample(include);

 const icons = JSON.parse(include('../../../../../.config/icon.json'));
 const selected = icons.filter(icon => icon.category === locals.category);

 for (const icon of selected) {
 %>
<%- sample(`${icon.name} [${icon.family}]`, '../../../core/template/ejs/icon/icon', { icon: icon }, true); %>

<% } %>
