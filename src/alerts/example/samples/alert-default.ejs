<%
 let data = {
   title: (locals.title === false) ? false : locals.title ? locals.title : 'Titre',
   text: locals.text || 'Description',
  };
 %>

<%- include('../../templates/ejs/alert.ejs', data); %>
