<%#
# paramètres checkbox

* checkbox.id (string, required): id du checkbox

* checkbox.attributes (object, optional): attributs sur l'input

* checkbox.label (string, optional): label du checkbox

%>
<% eval(include('../../../../core/index.ejs')); %>
<%
 let checkbox = locals.checkbox || {};
 let checkboxAttrs = checkbox.attributes || {};
 %>
<input <%- includeAttrs(checkboxAttrs); %> type="checkbox" id="<%= checkbox.id %>" name="<%= checkbox.id %>">
<%- include('../../../form/template/ejs/label', {label: {label:checkbox.label, hint: checkbox.hint, id: checkbox.id}} ); %>
<%- include('../../../form/template/ejs/message', {message: checkbox}); %>
