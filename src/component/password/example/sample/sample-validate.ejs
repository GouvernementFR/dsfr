<%
let data = {
  input: {value: 'x8A@'},
  messagesGroup: {
    messages: [
      {id: uniqueId('message'), text: getText('format.label', 'password'), type: 'hint'},
      {id: uniqueId('message'), text: getText('format.characters', 'password'), type: 'error'},
      {id: uniqueId('message'), text: getText('format.special', 'password'), type: 'valid'},
      {id: uniqueId('message'), text: getText('format.digit', 'password'), type: 'valid'}
    ]
  },
}
%>

<%- include('./password-register.ejs', {password: data}); %>
