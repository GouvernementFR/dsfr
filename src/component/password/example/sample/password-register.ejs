<%
let password = locals.password || {};

let data = {
  id: uniqueId('password'),
  ...password,
  input: {
    label: getText('password', 'password'),
    autocomplete: 'new-password',
    ...password.input
  },
  checkbox: password.checkbox || {},
  link: false,
  messagesGroup: {
    messages: [
      {id: uniqueId('message'), text: getText('format.label', 'password'), type: 'hint'},
      {id: uniqueId('message'), text: getText('format.characters', 'password'), type: 'info'},
      {id: uniqueId('message'), text: getText('format.special', 'password'), type: 'info'},
      {id: uniqueId('message'), text: getText('format.digit', 'password'), type: 'info'}
    ],
    ...password.messagesGroup},
}
%>

<%- include('../../template/ejs/password.ejs', {password: data}); %>
