<%
let password = locals.password || {};

let data = {
  id: uniqueId('password'),
  ...password,
  input: {label: getText('password', 'password'), autocomplete: 'new-password', ...password.input},
  checkbox: password.checkbox || {},
  link: false,
  format: {label: getText('format.label', 'password'), items: [{label: getText('format.characters', 'password'), state: 'info'}, {label: getText('format.special', 'password'), state: 'info'}, {label: getText('format.digit', 'password'), state: 'info'}], ...password.format},
}
%>

<%- include('../../template/ejs/password.ejs', {password: data}); %>
