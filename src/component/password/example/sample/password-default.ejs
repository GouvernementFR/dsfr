<%
let password = locals.password || {};

let data = {
  id: uniqueId('password'),
  ...password,
  input: {label: getText('password', 'password'), hint: 'texte de description additionel', ...password.input},
  button: {...password.button},
  format: {label: getText('format.label', 'password'), items: [{label: getText('format.characters', 'password'), state: 'info'}, {label: getText('format.digit', 'password'), state: 'info'}, {label: getText('format.special', 'password'), state: 'info'}]},
}
%>

<%- include('../../template/ejs/password.ejs', {password: data}); %>
