<%
let element = {
  path: 'radio/example/sample/radio-rich',
    data:locals.element || {}
}

element.data.name = id;
element.data.rich = true;

let fields = {
    elements: [
        { ...element, data: { ...element.data, id: id + '-1' } },
        { ...element, data: { ...element.data, id: id + '-2' } },
        { ...element, data: { ...element.data, id: id + '-3' } },
    ]
}
%>

<%- include('../../../form/example/sample/fieldset', fields) %>
