<%
    let fieldset = locals.fieldset || {};
    let data = JSON.parse(include('./data'));
    if (data.legend === undefined) data.legend = 'Légende pour l’ensemble de champs';
 %>

<%- include('../../template/ejs/fieldset', {fieldset: {...fieldset, data}}); %>
