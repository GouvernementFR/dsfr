<%
  let fieldset = locals.fieldset || {};
  let data = JSON.parse(include('./data'));
  if (!fieldset.legend) data.legend = 'Légende pour l’ensemble de champs';
%>

<%- include('../../template/ejs/fieldset/fieldset', {fieldset: {...fieldset, ...data}}); %>
