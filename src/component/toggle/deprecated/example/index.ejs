<% const sample = getSample(include); %>

<%- sample('Toggle simple avec bouton + libellé à droite', './sample/toggle-default.ejs', {toggle: {state: false}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à droite + texte d’aide', './sample/toggle-default.ejs', {toggle: {state: false, hint: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à droite + état', './sample/toggle-default.ejs', {toggle: {state: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à droite + état + texte d’aide', './sample/toggle-default.ejs', {toggle: {state: true, hint: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à droite + état + séparateur', './sample/toggle-default.ejs', {toggle: {state: true, border: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à droite + état + séparateur + texte d’aide', './sample/toggle-default.ejs', {toggle: {state: true, border: true, hint: true}}, true, './layout'); %>

<%- sample('Toggle simple disabled avec bouton + libellé à droite', './sample/toggle-default.ejs', {toggle: {state: false, disabled: true}}, true, './layout'); %>

<%- sample('Toggle simple disabled avec bouton + libellé à droite + état', './sample/toggle-default.ejs', {toggle: {state: true, disabled: true}}, true, './layout'); %>

<%- sample('Toggle simple disabled et pré-coché avec bouton + libellé à droite + état', './sample/toggle-default.ejs', {toggle: {state: true, disabled: true, checked:true}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à droite + séparateur', './sample/toggle-group.ejs', {toggle: {border:true, groupId: 'group-1-'}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à droite + séparateur + texte d’aide', './sample/toggle-group.ejs', {toggle: {border:true, groupId: 'group-1-', hint: true}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à droite + état + séparateur', './sample/toggle-group.ejs', {toggle: {border: true, state: true, groupId: 'group-2-'}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à droite + état + séparateur + texte d’aide', './sample/toggle-group.ejs', {toggle: {border: true, state: true, groupId: 'group-2-', hint: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à gauche', './sample/toggle-default.ejs', {toggle: {state: false, left: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à gauche + texte d’aide', './sample/toggle-default.ejs', {toggle: {state: false, left: true, hint: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à gauche + état', './sample/toggle-default.ejs', {toggle: {state: true, left: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à gauche + état + texte d’aide', './sample/toggle-default.ejs', {toggle: {state: true, left: true, hint: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à gauche + état + séparateur', './sample/toggle-default.ejs', {toggle: {state: true, border: true, left: true}}, true, './layout'); %>

<%- sample('Toggle simple avec bouton + libellé à gauche + état + séparateur + texte d’aide', './sample/toggle-default.ejs', {toggle: {state: true, border: true, left: true, hint: true}}, true, './layout'); %>

<%- sample('Toggle simple disabled avec bouton + libellé à gauche', './sample/toggle-default.ejs', {toggle: {state: false, left: true, disabled: true}}, true, './layout'); %>

<%- sample('Toggle simple disabled avec bouton + libellé à gauche + état', './sample/toggle-default.ejs', {toggle: {state: true, left: true, disabled: true}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à gauche + séparateur', './sample/toggle-group.ejs', {toggle: {border:true, groupId: 'group-3-', left: true}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à gauche + séparateur + texte d’aide', './sample/toggle-group.ejs', {toggle: {border:true, groupId: 'group-3-', left: true, hint: true}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à gauche + état + séparateur', './sample/toggle-group.ejs', {toggle: {border: true, state: true, groupId: 'group-4-', left: true}}, true, './layout'); %>

<%- sample('Groupe de toggles simple avec bouton + libellé à gauche + état + séparateur + texte d’aide', './sample/toggle-group.ejs', {toggle: {border: true, state: true, groupId: 'group-4-', left: true, hint: true}}, true, './layout'); %>

