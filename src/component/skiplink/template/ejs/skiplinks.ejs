<%#
# paramètres skiplink

* items (array, required) : Tableau de liens (href, label)

** items.href (string, required) : url ou ancre du lien

** items.label (string, required) : label du lien

%>
<% eval(include('../../../../core/index.ejs')); %>

<div class="<%= prefix %>-skiplinks">
  <nav role="navigation" aria-label="Accès rapide" class="<%= prefix %>-container">
    <ul class="<%= prefix %>-skiplinks__list">
      <%
        for (const item of locals.items) {
      %>
      <li>
        <a class="<%= prefix%>-link" href="<%= item.href %>" ><%= item.label %></a>
      </li>
      <% } %>
    </ul>
  </nav>
</div>
