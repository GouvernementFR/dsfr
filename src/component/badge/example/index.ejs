<% const sample = getSample(include); %>

<%- sample('badge seul', './sample/badge-default', {}, true); %>
<%- sample('badge seul avec icône', './sample/badge-default', {badge: {icon: 'information-fill'}}, true); %>

<p>Il est conseillé d'ajouter une balise span avec la classe fr-ellipsis à l'interieur du badge pour que celui ci reste sur une seule ligne, avec le texte coupé par des points de suspensions.</p>
<%- sample('badge sur une seule ligne - ellipsis', './sample/badge-ellipsis', {}, true); %>

<%- sample('badge seul SM', './sample/badge-sm', {}, true); %>
<%- sample('badge seul SM avec icône', './sample/badge-sm', {badge: {icon: 'information-fill'}}, true); %>

<%- sample('Groupe de badges', './sample/badge-default', {badge: {group: true}}, true); %>
<%- sample('Groupe de badges SM', './sample/badge-sm', {badge: {group: true}}, true); %>

