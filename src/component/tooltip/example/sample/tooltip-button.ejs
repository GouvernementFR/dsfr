<%
let tooltip = locals.tooltip || {};
let data = {
  id: uniqueId('tooltip'),
  content: lorem(),
  ...tooltip
}

let button = {
  label: tooltip.triggerText || 'Infobulle',
  classes: ['fr-btn--tooltip'],
  tooltip: data
}
%>

<%- include('../../../../component/button/template/ejs/button.ejs', {button: button}); %>
