<%
let tooltip = locals.tooltip || {};
let tooltipId = uniqueId('tooltip');
let data = {
  id: tooltipId,
  content: lorem(),
  ...tooltip
};

let input = {};

input.valid = validData(input.valid);
input.error = errorData(input.error);
input.hint = hintData(input.hint);
input.id = uniqueId('tooltip');

input.type = 'text';
input.label = (getText('label', 'form') + ' ' + getText('title', 'input-base').toLowerCase());
input.addon = true;
input.button = {
  label:'Envoyer',
  type: 'submit',
  attributes: {
    'aria-describedby': tooltipId
  }
};

%>

<%- include('../../../input/input-base/template/ejs/input-group', { input: input }); %>
<%- include('../sample/tooltip-default', {tooltip: data}); %>
