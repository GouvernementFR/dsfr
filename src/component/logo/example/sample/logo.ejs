<%
 let logo = locals.logo || {};
 if (logo.title === undefined) logo.title = 'République<br>française';
 if (logo.href === undefined) logo.href = '#';
 %>

<%- include('../../template/ejs/logo', {logo: locals.logo});%>
