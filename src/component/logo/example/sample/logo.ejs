<%
 let data = locals.data || {}
 if (locals.title === undefined) data.title = 'République<br>française';
 if (locals.href === undefined) data.href = '#';
 %>

<%- include('../../template/ejs/logo', data);%>
