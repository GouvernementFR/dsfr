<%

let data = locals;

const dataAccordion = {
    label: locals.label || 'Intitulé accordéon',
    content: include('../../../tab/example/sample/tabs-default', data),
    collapseId: uniqueId('tab')
}

%>

<%- include('../../template/ejs/accordion', dataAccordion); %>
