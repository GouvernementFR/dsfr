<%
const connect = locals.connect || {};
const data = {
  ...locals.connect,
  type: 'plus',
  link: connect.link || {
    label: getContent('link-plus'),
    href: getContent('href-plus')
  }
}

%>
<%- include('connect-default.ejs', {connect: data}) %>
