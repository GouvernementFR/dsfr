<%
const connect = locals.connect || {};
const data = {
  ...locals.connect,
  type: 'plus',
  link: connect.link || {
    label: getText('link-plus'),
    href: getText('href-plus')
  }
}

%>
<%- include('connect-default.ejs', {connect: data}) %>
