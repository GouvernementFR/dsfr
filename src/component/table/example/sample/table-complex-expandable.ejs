<%
const table = locals.table || {};
const data = JSON.parse(include('../data/data-complex-expandable.json.ejs', { table: table.table }));

const option = {
    ...table,
    ...data
}
%>

<%- include('./table-default', {table: option}); %>
