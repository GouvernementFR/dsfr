<%
let table = locals.table || {};
let data = {
  caption: table.caption || undefined,
  bordered: table.bordered || false,
  noScroll: table.noScroll || false,
  noCaption: table.noCaption || false,
  captionBottom: table.captionBottom || false,
  accent: table.accent || undefined,
  layout: table.layout,
  col: table.col || 2,
  row: table.row || 2,
}

data.data = new Array(data.row);
for(let row = 0; row < data.row; row++) {
  data.data[row] = new Array(data.col);
  for(let col = 0; col < data.col; col++) {
    if(row == 0) data.data[0][col] =  'th' + col;
    else data.data[row][col] = lorem(null , Math.floor(Math.random() * 50));
  }
}
data.col = undefined;
data.row = undefined;
data.data = undefined;
%>

<%- include('../../template/ejs/table', {table: data}); %>
