<%
const sample = getSample(include);
%>

<%- sample('Mise en page avec tiroir à droite', './sample/drawer-right', {layout: {background: 'contrast'}}, true ); %>

<%- sample('Mise en page avec fond contrasté', './sample/drawer-left', {layout: {background: 'contrast'}}, true ); %>

<%- sample('Tiroir sans collapse', './sample/drawer-static', {layout: {background: 'alt'}}, true ); %>

<%- sample('Tiroirs dans une mise en page', './sample/drawer-full', {}, true ); %>
