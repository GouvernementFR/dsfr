<%

let data = locals;

if (!isNaN(data.tabs)) {
  const length = data.tabs;
  data.tabs = [];
  for (let i = 0; i < length; i++) {
     data.tabs[i] = {
      label: `Label Tab ${i + 1}`,
      content: include('../../../accordions/example/sample/accordion'),
    }
  }
}

%>

<%- include('../../template/ejs/tabs', data) %>
