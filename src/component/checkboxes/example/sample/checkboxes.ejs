<%

let checkboxAttrs = locals.checkboxAttrs || [];
let describedby = [];
if (locals.error !== undefined) describedby.push('checkboxes-error-desc-error');
if (locals.valid !== undefined) describedby.push('checkboxes-valid-desc-valid');
if (describedby.length > 0) checkboxAttrs['aria-describedby'] = describedby.join(' ');
let element = {
  path: 'checkboxes/example/sample/checkbox',
    data:locals.element || {}
}

let fields = {
    choice: true,
    elements: [
        { ...element, checkboxAttrs: checkboxAttrs, data: { ...element.data, checkboxAttrs: checkboxAttrs, id: id + '-1' } },
        { ...element, data: { ...element.data, checkboxAttrs: checkboxAttrs, id: id + '-2' } },
        { ...element, data: { ...element.data, checkboxAttrs: checkboxAttrs, id: id + '-3' } },
    ]
}
%>

<%- include('../../../forms/example/sample/fieldset', fields) %>
