<% if (locals.title !== undefined && locals.title.length) { %>
  <h4><%= title %></h4>
  <% } %>
  <div class="<%= prefix %>-mb-6v" >
    <button class="fr-btn" onclick="const component = document.createElement('div'); component.className = prefix + '-my-8v'; component.innerHTML = decodeURIComponent('<%- encodeURIComponent(component) %>'); this.after(component);"> click me</button>

</div>
  <% if (locals.snippet !== undefined) { %>
  <div class="<%= prefix %>-mb-12v" >
      <%- snippet %>
  </div>
  <% } %>

