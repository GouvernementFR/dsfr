<%#
# paramètres du bouton de la modale

* modal.id (string, required) : id de la modale

* modal.label (string, required) : label du bouton de la modale

* modal.opened (boolean, optional) : si true la modale est ouverte

%>
<% eval(include('../../../../core/index.ejs')); %>

<%
    let modal = locals.modal || {};
    const attributes = {};
    attributes[`data-${prefix}-opened`] = modal.opened === true;
    attributes['aria-controls'] = modal.id;

%>

<%- include("../../../button/template/ejs/button", {button: {label: modal.label, attributes: attributes}}) %>
