<%#
# paramètres du bouton de la modale

* modalBtn.id (string, required) : id de la modale

* modalBtn.label (string, required) : label du bouton de la modale

* modalBtn.opened (boolean, optional) : si true la modale est ouverte

%>
<% eval(include('../../../../core/index.ejs')); %>

<%
    let modalBtn = locals.modalBtn || {};
    const attributes = {};
    attributes[`data-${prefix}-opened`] = modalBtn.opened === true;
    attributes['aria-controls'] = modalBtn.id;

%>

<%- include("../../../button/template/ejs/button", {button: {label: modalBtn.label, attributes: attributes}}) %>
