<%
const sample = getSample(include);

let dataText = {
    title: 'Titre de la modale',
    titleIcon: 'arrow-right-line',
    body: '../../example/sample/body/text',
}

let dataForm = {
    title: 'Titre de la modale',
    titleIcon: 'arrow-right-line',
    body: '../../example/sample/body/form',
}

let dataFooterButtons = {
    title: 'Titre de la modale',
    titleIcon: 'arrow-right-line',
    body: '../../example/sample/body/text',
    footer: '../../example/sample/footer/buttons',
}
%>

<%- sample('Modale simple', './sample/modal-default', { ...dataText, label: 'Modal simple'}, true ); %>

<%- sample('Modale SM', './sample/modal-default', { ...dataText, size: 'sm', label: 'Modal SM'}, true ); %>

<%- sample('Modale LG', './sample/modal-default', { ...dataText, size: 'lg', label: 'Modal LG'}, true ); %>

<%- sample('Modale formulaire', './sample/modal-default', { ...dataForm, label: 'Modal formulaire'}, true ); %>

<%- sample('Modal avec zone d\'action', './sample/modal-default', { ...dataFooterButtons, label: 'Modale avec zone d\'action'}, true ); %>

<%- sample('Modal avec zone d\'action ancré en haut', './sample/modal-default', { ...dataFooterButtons, label: 'Modale ancrée en haut', top: true}, true ); %>
