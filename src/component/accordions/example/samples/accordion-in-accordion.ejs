<%
    const data = {
        label: locals.label || 'Intitulé accordéon',
        content: include('./accordion', {button: true}),
        collapseId: uniqueId('accordion')
    }
%>

<%- include('../../template/ejs/accordion', data); %>
