<%
let footer = locals.footer || {};
let data = {}

if(footer.id) data.id = footer.id || uniqueId('footer');
else data.id = uniqueId('footer');

if(footer.top === true) data.top = { categories: {label: 'Nom de la catégorie', link: 'Lien de navigation'}};

if(footer.brand === true) data.brand = {logo:{ title: 'Intitulé<br>officiel'}};

if(footer.operator === true) data.brand.operator = imgData('img/placeholder.16x9.png', 'rendered');

if(footer.content === true) data.content = {
  desc: lorem(),
  links: [
    {label: 'legifrance.gouv.fr'},
    {label: 'gouvernement.fr'},
    {label: 'service-public.fr'},
    {label: 'data.gouv.fr'}
  ]
};

if(footer.partner === true) data.partner = {
  title: 'Nos partenaires',
  mainPartner: imgData('img/placeholder.16x9.png', 'rendered'),
  subPartners: [
      imgData('img/placeholder.16x9.png', 'rendered'),
      imgData('img/placeholder.16x9.png', 'rendered'),
      imgData('img/placeholder.16x9.png', 'rendered')
  ]
};

if(footer.bottom === true) data.bottom = {
  links: [
    {label: 'Plan du site'},
    {label: 'Accessibilité : non/partiellement/totalement conforme'},
    {label: 'Mentions légales'},
    {label: 'Données personnelles'},
    {label: 'Gestion des cookies'}
  ],
  copyright: 'Sauf mention contraire, tous les contenus de ce site sont sous <a href="https://github.com/etalab/licence-ouverte/blob/master/LO.md" target="_blank">licence etalab-2.0</a>'
};

console.log(data)
%>

<%- include('../../template/ejs/footer', {footer: data}); %>
