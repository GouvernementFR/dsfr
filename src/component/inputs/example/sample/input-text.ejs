<%
    let data = JSON.parse(include('../../../forms/example/sample/data'));

    data.type = locals.type || 'text';
    data.label = 'Label champs de saisie';

%>
<% if (locals.group === true) { %>
<%- include('../../template/ejs/input-group', data); %>
<% }
else { %>
<%- include('../../template/ejs/input', data); %>
<% } %>
