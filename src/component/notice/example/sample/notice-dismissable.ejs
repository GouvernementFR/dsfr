<%
let notice = locals.notice || {};
const id = uniqueId('notice');
let data = {
  ...notice,
  id: id,
  button: { label: 'Masquer le message', attributes: {title: 'Masquer le message', onclick: `const notice = document.getElementById('${id}'); notice.parentNode.removeChild(notice)`}},
  dismissable: true
};
 %>

<%- include('notice-default.ejs', {notice:data}); %>
