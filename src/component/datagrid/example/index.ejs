<% const sample = getSample(include); %>

<%- sample(getText('sample.default', 'datagrid'), './sample/datagrid-default', {}, true); %>

<%- sample(getText('sample.scrollable', 'datagrid'), './sample/datagrid-scrollable', {}, true); %>

<%- sample(getText('sample.fixed-width', 'datagrid'), './sample/datagrid-fixed-width', {}, true); %>

<%- sample(getText('sample.expandable', 'datagrid'), './sample/datagrid-expandable', {}, true); %>
