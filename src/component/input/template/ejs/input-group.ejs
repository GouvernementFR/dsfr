<%#
# paramètres input group

* input.valid (string, optionnal) : message de validation

* input.error (string, optionnal) : message d'erreur

* input.disabled (boolean, optionnal)

%>
<% eval(include('../../../../core/index.ejs')); %>

<%
 let groupClasses = [prefix + '-input-group'];

 if (input.error !== undefined) groupClasses.push(prefix + '-input-group--error');
 if (input.disabled !== undefined) groupClasses.push(prefix + '-input-group--disabled');
 if (input.valid !== undefined) groupClasses.push(prefix + '-input-group--valid');
 %>

<div <%- includeClasses(groupClasses); %>>
    <%- include('./input'); %>
</div>
