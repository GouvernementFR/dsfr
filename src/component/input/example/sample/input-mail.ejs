<%
   let input = locals.input || {};
   let attributes = input.attributes || {};
    attributes.pattern = '[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$';
    attributes['aria-label'] = 'pattern: xxx';

let data = {
    ...input,
    type:'mail',
    attributes: attributes
}
%>

<%- include('./input-text', {input: data}); %><!-- aria-label mandatory to explain patter -->