<%
let input = locals.input || {}

input.valid = validData(input.valid);
input.error = errorData(input.error);
input.hint = hintData(input.hint);

input.type = input.type || 'text';
input.label = input.label || (getText('label', 'form') + ' ' + getText('title', 'input-base').toLowerCase());

%>
<%- include('../../template/ejs/input-group', { input: input }); %>
