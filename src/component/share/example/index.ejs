<% const sample = getSample(include); %>

<%- sample('Méta données', './sample/meta-share', {}, true); %>

<%- sample('Boutons de partage par défaut', './sample/share-default', {}, true); %>

<%- sample('Boutons de partage version inactive', './sample/share-default', {share:{disabled: true}}, true); %>
