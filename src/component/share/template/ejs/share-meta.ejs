<%#
# paramètres share meta

* meta (array, required): tableau de meta tag

* meta.property (string, required) :  meta property

* meta.content (string, optional) : meta content

* meta.name (string, optional) : meta name

%>
<% eval(include('../../../../core/index.ejs')); %>
<% let meta = locals.meta || {}; %>

<% for (const item of locals.meta) { %>
  <% if(item.property) { %>
    <meta property="<%= item.property %>" content="<%= item.content %>">
  <% } else if(item.name) { %>
    <meta name="<%= item.name %>" content="<%= item.content %>">
  <% }
} %>
