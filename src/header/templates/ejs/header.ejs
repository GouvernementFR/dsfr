<%#
# paramètres header

// TODO: documentation

header = {
    brand: {
        logo: { voir logo },
        operator: {
            img: 'url de l'img', // TODO: potentiellement à revoir
            alt: 'texte alternatif',
            style: '' // TODO: taille de l'image gérée autrement
        },
        service: {
            title: '',
            tagline: ''
        }
    },
    tools: {
        links: { // TODO: à revoir avec groupe de links
            items: [
                { voir links }
            ]
        },
        search: { voir search }
    },
    navigation: {
        voir navigation
    },
    navbar: {
        menu: {
            id: '',
            modalId: '',
            label: ''
        }
        search: {
            id: '',
            modalId: '',
            label: ''
        }
    }
}

%>
<% eval(include('../../../core/index.ejs')); %>

<header role="banner" class="<%= prefix %>-header">

    <%- include('./body');%>

    <% if (locals.navigation || (locals.tools && tools.links)) { %>
        <%- include('./menu');%>
    <% } %>

</header>
