<%
const name = { 
    id: uniqueId('button-and-firstname-disabled'),
    label: getText('sample.button.firstname-disabled', 'name-firstname'),
    items:[
        {
            type: 'input',
            template: 'input-group',
            label: getText('label.name', 'name-firstname'),
            value: 'name'
        },
        {
            type: 'input',
            template: 'input-group',
            label: getText('label.firstname', 'name-firstname'),
            value: 'firstname',
            disabled: true
        },
        {
            type: 'button',
            template: 'button',
            label: getText('label.add-firstname', 'name-firstname'),
            kind: 3,
            size: 'sm',
            disabled: true
        },
        {
            type: 'checkbox',
            template: 'checkbox-group',
            label: getText('label.no-firstname', 'name-firstname'),
            value: 'firstname-disabled',
            checked: true,
            size: 'sm',
            attributes: {onclick: `const formGroup = this.closest('.fr-form-group'); const firstnameFieldset = formGroup && formGroup.previousElementSibling.previousElementSibling.firstElementChild; const buttonFieldset = formGroup && formGroup.previousElementSibling.firstElementChild; if (this.checked) { firstnameFieldset.setAttribute('disabled', ''); buttonFieldset.setAttribute('disabled', ''); } else { firstnameFieldset.removeAttribute('disabled'); buttonFieldset.removeAttribute('disabled'); }`},
        } 
    ]
}
%>

<%- include('../../template/ejs/name-firstname', {name: name}); %>