<%
let society = locals.society || {};
const countriesData = JSON.parse(include('../../../address/example/sample/data/country.ejs'));

const data = {
  id: uniqueId('society-address'),
  label: getText('fieldset.address', 'society'),
  items: [
    {
      type: 'select',
      template: 'select',
      data: {
        name: 'society-type',
        label: getText('label.type', 'society'),
        placeholder: getText('option.placeholder', 'select'),
        options: [
          {value: 'EI', label: getText('type.EI', 'society')},
          {value: 'EURL', label: getText('type.EURL', 'society')},
          {value: 'SARL', label: getText('type.SARL', 'society')},
          {value: 'SASU', label: getText('type.SASU', 'society')},
          {value: 'SAS', label: getText('type.SAS', 'society')},
          {value: 'SA', label: getText('type.SA', 'society')},
          {value: 'SNC', label: getText('type.SNC', 'society')},
          {value: 'SCS', label: getText('type.SCS', 'society')},
          {value: 'SCA', label: getText('type.SCA', 'society')},
        ]
      }
    },
  ]
}
%>

<%- include('./society-default', {society: data}); %>