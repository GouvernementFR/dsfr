<%
const data = {
  id: uniqueId('date-error'),
  error: [
    {label: getText('error.day', 'date')},
    {label: getText('error.month', 'date')},
    {label: getText('error.year', 'date')}
  ],
};
%>

<%- include('./date-default', {date: data}); %>
