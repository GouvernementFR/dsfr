<%
const date = locals.date || {};
const id = date.id || uniqueId('date-default');

const data = {
  id: id,
  legend: date.legend || getText('legend.birthdate', 'date'),
  hint: date.hint || getText('hint.birthdate', 'date'),
  error: date.error,
  elements:[
    {
      type: 'input',
      inline: true,
      modifier: 'number',
      data: {
        id: `${id}-bday-day`,
        name: 'bday-day',
        label: getText('option.day', 'date'),
        hint: getText('hint.day', 'date')
      }
    },
    {
      type: 'input',
      inline: true,
      modifier: 'number',
      data: {
        id: `${id}-bday-month`,
        name: 'bday-month',
        label: getText('option.month', 'date'),
        hint: getText('hint.month', 'date')
      }
    },
    {
      type: 'input',
      inline: true,
      grow: true,
      modifier: 'year',
      data: {
        id: `${id}-bday-year`,
        name: 'bday-year',
        label: getText('option.year', 'date'),
        hint: getText('hint.year', 'date')
      }
    }
  ]
};
%>

<%- JSON.stringify(data); %>
