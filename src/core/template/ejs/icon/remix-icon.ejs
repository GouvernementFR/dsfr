<%#
# paramètres icon (remixicon)

* icon.icon (string, required): nom de l'icone

* icon.size (string, required) [default: 'md']: taille de l'icone (sm|md|lg)

%>
<% eval(include('../../../index.ejs')); %>
<% let icon = locals.icon || {} %>

<span class="<%= prefix %>-fi-<%= icon.icon %> <% switch(icon.size) {
  case 'sm': %><%= prefix %>-fi--sm<% break;
  case 'lg': %><%= prefix %>-fi--lg<% break;
} %>" aria-hidden="true"></span>
