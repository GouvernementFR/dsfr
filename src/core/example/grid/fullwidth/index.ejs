
<style>
  .col-demo {
    box-shadow: inset 0 0 0 1px var(--border-default-blue-france);
    text-align: center;
    background-color: var(--background-action-low-blue-france);
    padding: 16px 0;
  }

  .<%= prefix %>%>-grid-row > * > .<%= prefix %>%>-grid-row > * > .col-demo {
    background-color: var(--background-action-high-blue-france);
    color: var(--text-inverted-blue-france);
  }
</style>

<% const sample = getSample(include); %>

<%- sample('Grille avec conteneur full page (dashboard)', '../sample/container', { grid: {fullWidth: true, classes: [ `${prefix}-grid-row--center` ], gutters: true, cols: [{ length: 6, col: { first: 12, sm: 10, md: 8, lg: 6, xl: 4, xxl: 3, xxxl: 2 } }]}}, true); %>
