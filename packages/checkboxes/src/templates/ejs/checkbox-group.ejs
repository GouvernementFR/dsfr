<%#
# paramètres checkbox group

* error (string, optionnal) : message d'erreur

* size (string, optionnal) : la valeur sm permet d'opter pour de petites cases à cocher

%>
<% eval(include('../../../../core/index.ejs')); %>
<%
let groupClasses = [prefix + '-checkbox-group'];

if (locals.error !== undefined) groupClasses.push(prefix + '-checkbox-group--error');
if (locals.valid !== undefined) groupClasses.push(prefix + '-checkbox-group--valid');
//if (locals.disabled !== undefined) groupClasses.push(prefix + '-checkbox-group--disabled');

switch(locals.size) {
    case 'sm' :
        groupClasses.push(prefix + '-checkbox-group--sm');
        break;

}
%>

<div <%- includeClasses(groupClasses); %>>
    <%- include('./checkbox'); %>
</div>
