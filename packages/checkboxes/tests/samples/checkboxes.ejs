<%
let element = {
  path: 'checkboxes/tests/samples/checkbox',
    data:locals.element || {}
}

let fields = {
    elements: [
        { ...element, data: { ...element.data, id: id + '-1' } },
        { ...element, data: { ...element.data, id: id + '-2' } },
        { ...element, data: { ...element.data, id: id + '-3' } },
    ]
}
%>

<%- include('../../../forms/tests/samples/fieldset', fields) %>