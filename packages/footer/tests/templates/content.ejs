<div class="<%= prefix %>-footer__content" >
	<p class="<%= prefix %>-footer__content-desc"><%- desc %></p>
	<ul class="<%= prefix %>-footer__content-links">
		<%
			for (let i = 0; i < links.length; i++) {
		%>
		<li>
			<a href="http://<%= links[i].label %>"><%= links[i].label %></a>
		</li>
		<% } %>
	</ul>
</div>