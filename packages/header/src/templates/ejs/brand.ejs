<div class="<%= prefix %>-header__brand">
    <%
    switch(typeof locals.logo) {
    case 'string': %>
    <%- include(locals.logo); %>
    <% break;

    case 'object': %>
    <%- include('../../../../logo/src/templates/ejs/logo', locals.logo); %>
    <% break;
    }
    %>

</div>
