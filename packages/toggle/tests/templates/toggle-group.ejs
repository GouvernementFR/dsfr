<ul class="<%= prefix %>-toggle-list">
  <%
    for (let i = 0; i < 5; i++) {
      let groupToggleId = 'group-' + i
  %>
    <li>
        <%- include('./toggle', {toggleId: hint ? 'toggle-hint-' + i : 'toggle-' + i} ); %>
    </li>
  <% } %>
</ul>