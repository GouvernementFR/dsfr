
    <div class="<%= prefix%>-container">

      <h4 class="<%= prefix %>-h4">Bouton simple - Light scheme</h4>
      <div class="flex justify-between items-center <%= prefix%>-scheme-light-white <%= prefix%>-scheme-border <%= prefix%>-scheme-border--left" style="max-width: 55rem;padding: 3rem 10rem;">

        <button class="<%= prefix%>-btn">
          Label bouton
        </button>

        <button disabled class="<%= prefix%>-btn">
          Label bouton désactivé
        </button>
        <br>
        <br>
        <button class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
          Label bouton
        </button>

        <button disabled class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
          Label bouton désactivé
        </button>

      </div>

      <h4 class="<%= prefix %>-h4">Bouton simple - Light scheme grey 100</h4>
      <div class="<%= prefix%>-scheme-light-grey-100 <%= prefix%>-scheme-border <%= prefix%>-scheme-border--left" style="padding: 3rem">

        <div class="<%= prefix%>-grid-row <%= prefix%>-grid-row--gutters <%= prefix%>-grid-row--center" >
          <div class="<%= prefix%>-col-8" style="padding: 3rem">
            <button class="<%= prefix%>-btn">
              Label bouton
            </button>

            <button disabled class="<%= prefix%>-btn">
              Label bouton désactivé
            </button>
            <br>
            <br>
            <button class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
              Label bouton
            </button>

            <button disabled class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
              Label bouton désactivé
            </button>
          </div>



          <div class="<%= prefix%>-col-8 <%= prefix%>-scheme-soft-pink-light <%= prefix%>-scheme-border <%= prefix%>-scheme-border--left" style="padding: 3rem">
            <button class="<%= prefix%>-btn">
              Label bouton
            </button>

            <button disabled class="<%= prefix%>-btn">
              Label bouton désactivé
            </button>
            <br>
            <br>
            <button class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
              Label bouton
            </button>

            <button disabled class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
              Label bouton désactivé
            </button>

          </div>

          <div class="<%= prefix%>-col-8 <%= prefix%>-scheme-light-white <%= prefix%>-scheme-border <%= prefix%>-scheme-border--left" style="padding: 3rem">
            <button class="<%= prefix%>-btn">
              Label bouton
            </button>

            <button disabled class="<%= prefix%>-btn">
              Label bouton désactivé
            </button>
            <br>
            <br>
            <button class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
              Label bouton
            </button>

            <button disabled class="<%= prefix%>-btn <%= prefix%>-btn--secondary">
              Label bouton désactivé
            </button>
          </div>
        </div>
      </div>

      <h4 class="<%= prefix %>-h4">Bouton simple - Chromatic scheme</h4>
      <div class="flex justify-between items-center <%= prefix%>-scheme-soft-brown <%= prefix%>-scheme-border <%= prefix%>-scheme-border--left" style="max-width: 55rem;padding: 3rem 10rem;">

        <button class="<%= prefix%>-btn">
          Label bouton
        </button>

        <button disabled class="rf-btn">
          Label bouton désactivé
        </button>
        <br>
        <br>
        <button class="rf-btn rf-btn--secondary">
          Label bouton
        </button>

        <button disabled class="rf-btn rf-btn--secondary">
          Label bouton désactivé
        </button>

      </div>

      <h4 class="<%= prefix %>-h4">Bouton simple - Dark scheme</h4>
      <div class="flex justify-between items-center rf-scheme-dark-grey-800 rf-scheme-border rf-scheme-border--left" style="max-width: 55rem;padding: 3rem 10rem;">

        <button class="rf-btn">
          Label bouton
        </button>

        <button disabled class="rf-btn">
          Label bouton désactivé
        </button>
        <br>
        <br>
        <button class="rf-btn rf-btn--secondary">
          Label bouton
        </button>

        <button disabled class="rf-btn rf-btn--secondary">
          Label bouton désactivé
        </button>

      </div>
    </div>
