<%
const sample = getSample(include);

let dataText = {
    title: 'Titre de la modale',
    titleIcon: 'arrow-right-line',
    body: '../../../example/samples/body/text',
}

let dataForm = {
    title: 'Titre de la modale',
    titleIcon: 'arrow-right-line',
    body: '../../../example/samples/body/form',
}

let dataFooterButtons = {
    title: 'Titre de la modale',
    titleIcon: 'arrow-right-line',
    body: '../../../example/samples/body/text',
    footer: '../../../example/samples/footer/buttons',
}
%>

<%- sample('Modal simple', './samples/modal-default', { ...dataText, label: 'Modal simple'}, true ); %>

<%- sample('Modal SM', './samples/modal-default', { ...dataText, size: 'sm', label: 'Modal SM'}, true ); %>

<%- sample('Modal LG', './samples/modal-default', { ...dataText, size: 'lg', label: 'Modal LG'}, true ); %>

<%- sample('Modal formulaire', './samples/modal-default', { ...dataForm, label: 'Modal formulaire'}, true ); %>

<%- sample('Modal avec zone d\'action', './samples/modal-default', { ...dataFooterButtons, label: 'Modal avec zone d\'action'}, true ); %>
