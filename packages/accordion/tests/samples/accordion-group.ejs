<%

let items = [];

let accordion = {
    type: 'accordion',
    items: items
}

let collapseId;

let data = {
  items: [accordion, accordion, accordion, accordion, accordion, accordion],
  groupCollapseId: locals.groupCollapseId
}
%>

<%- include('../templates/accordion-group', data); %>