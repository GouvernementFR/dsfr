/* eslint no-unused-vars: "off" */
class Instantiater {
  constructor () {
    this.instances = [];
    this.resizes = [];
  }

  add (selector, factory, isResizing) {

  }

  resize () {
    // this.isLarge = window.matchMedia('(min-width: 62em)').matches TODO: checker le breakpoint et distribuer lors du resize. boolean ? sm: 1 // md: 2 // lg: 4 // xl: 8

  }
}
