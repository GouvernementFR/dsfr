<% const sample = getSample(include); %>

<%- sample('Headings', './samples/headings', {}, true); %>

<%- sample('Displays', './samples/displays', {}, true); %>

<%- sample('Style Chapo', './samples/p', { size: 'lead' }, true); %>

<%- sample('Style LG', './samples/p', { size: 'lg' }, true); %>

<%- sample('Style MD', './samples/p', { size: 'md' }, true); %>

<%- sample('Style SM', './samples/p', { size: 'sm' }, true); %>

<%- sample('Style XS', './samples/p', { size: 'xs' }, true); %>

<%- sample('Graisse light', './samples/p', { weight: 'light' }, true); %>

<%- sample('Graisse regular', './samples/p', { weight: 'regular' }, true); %>

<%- sample('Graisse bold', './samples/p', { weight: 'bold' }, true); %>

<%- sample('Font Spectral, graisse regular', './samples/p', { weight: 'regular', alt:true }, true); %>

<%- sample('Font Spectral, graisse extra-bold', './samples/p', { weight: 'xx-bold', alt:true }, true); %>

<%
let ol = {
  classes: [prefix + '-list'],
  lis:[{
      lis:[{
        lis:[{}, {}, {}]
      }, {}, {}]
    }, {}, {}
  ]}
  let ul = {
    classes: [prefix + '-list'],
    lis:[{
      lis:[{}, {}, {}]
      }, {}, {}
  ]}
%>
<%- sample('Test de liste non-ordonnée:', './samples/ul', ul, true); %>

<%- sample('Test de liste ordonnée :', './samples/ol', ol, true); %>