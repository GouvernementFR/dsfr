<%#
# Paramètres pagination

// TODO: documentation

%>
<% eval(include('../../../../core/index.ejs')); %>

<!--
-> A div can be used with role navigation
<div class="<%= prefix%>-pagination" role="navigation" aria-label="Pagination">
-->
<nav class="<%= prefix %>-pagination" aria-label="Pagination">
    <ul class="<%= prefix %>-pagination__list">
        <%
            let disabled = pages[0].active === true;
        %>
        <% if (locals.first !== undefined) { %><%- include('./nav', {...first, type:'first', disabled: disabled}); %><% } %>
        <% if (locals.prev !== undefined) { %><%- include('./nav', {...prev, type:'prev', disabled: disabled}); %><% } %>

        <%
            let page;
            for (let i = 0; i < pages.length; i++) {
              page = pages[i];
        %>
            <%- include('./item', page); %>
        <% }%>

        <%
            disabled = pages[pages.length - 1].active === true;
        %>
        <% if (locals.next !== undefined) { %><%- include('./nav', {...next, type:'next', disabled: disabled}); %><% } %>
        <% if (locals.last !== undefined) { %><%- include('./nav', {...last, type:'last', disabled: disabled}); %><% } %>
    </ul>
</nav>
