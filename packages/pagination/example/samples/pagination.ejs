<%
    let pages = [];
    let i;

    for (i = 1; i < 4; i++) pages.push({label:i, title:'Page ' + i, href:'#', fromMd: i > 2});
    pages.push({label:'…', fromMd:true});
    for (i = 130; i < 133; i++) pages.push({label:i, title:'Page ' + i, href:'#', fromMd: i < 132});

    if (locals.index !== undefined) pages[index].active = true;
    locals.title = undefined;

    let data = {
        first: { label: 'Première page', href:'#', labelFromLg: true },
        prev: { label: 'Page précédente', text:'Précédente', href:'#' },
        next: { label: 'Page suivante', text:'Suivante', href:'#' },
        last: { label: 'Dernière page', href:'#', labelFromLg: true },
        pages : pages
    }
%>

<%- include('../../src/templates/ejs/pagination', data); %>
