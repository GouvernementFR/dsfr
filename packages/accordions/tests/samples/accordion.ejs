<%
  const paragraph = '<p>Qui elit in aliqua exercitation. Sint exercitation incididunt duis eu mollit sunt id dolor ullamco excepteur adipisicing deserunt. Dolore mollit ullamco laboris nulla qui ad officia. Ad deserunt ex magna esse culpa ea occaecat sit tempor.</p>';
  let content = '';

  if (locals.title) content += '<h4>Titre niveau 4</h4>';

  const length = locals.paragraphs || 1;
  for (let i = 0; i < length; i++) content += paragraph;

  if(locals.image) content += include('../../../core/tests/medias/samples/img') + paragraph;

  if(locals.button) content += include('../../../buttons/tests/samples/button-default');

  const data = {
    label: locals.label || 'Intitulé accordéon',
    content: content,
    collapseId: uniqueId('accordion')
  }

%>


<%- include('../templates/accordion', data); %>
