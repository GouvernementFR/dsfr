
<%
let sizes = {
    small : {size:'sm', label: 'Label bouton SM'}
};
let data = {
    tabs: 4,
    starterId: 1,
    icon: 'checkbox-line',
    iconPlace: 'left',
    sizes: sizes
};

let data2 = {
    tabs: 2,
    starterId: 5
};

let data3 = {
    tabs: 2,
    starterId: 7
};

let samples = [
    { title:'tabs 4 col', data:data},
    { title:'tabs 2 col', data:data2},
    { title:'tabs dans tabs', data:data3}
]

%>

<% sample = samples[0] %>
<h4><%= sample.title %></h4>
<%- include('./samples/tabs-default', sample.data) %>

<% sample = samples[1] %>
<h4><%= sample.title %></h4>
<%- include('./samples/tabs-default', sample.data) %>
<%- snippet(include('./samples/tabs-default', sample.data)); %>


<% sample = samples[2] %>
<h4><%= sample.title %></h4>
<%- include('./samples/tabs-in-tabs', sample.data) %>
<%- snippet(include('./samples/tabs-in-tabs', sample.data)); %>


