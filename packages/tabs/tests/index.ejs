<%

let samples = [
    { sample:'./samples/tabs-default', title:'tabs 4 col', data:{ tabs: 2, icon: 'checkbox-line' }},
    { sample:'./samples/tabs-default',title:'tabs 2 col', data: { tabs: 2 }},
    { sample:'./samples/tabs-in-tabs',title:'tabs dans tabs'},
    { sample:'./samples/tabs-accordions',title:'accordéon dans tabs',data: { tabs: 2 }}
]

%>

<% for (let i = 0; i < samples.length; i++) { %>

<h4 class="<%= prefix %>-h4"><%= samples[i].title %></h4>
<%- include(samples[i].sample, samples[i].data) %>
<%- snippet(include(samples[i].sample, samples[i].data)); %>
<% } %>

