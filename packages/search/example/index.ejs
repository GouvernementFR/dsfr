<% const sample = getSample(include); %>

<%- sample('Barre de recherche MD (par défaut)', './samples/search-default.ejs', {}, true); %>

<%- sample('Barre de recherche LG', './samples/search-lg.ejs', {}, true); %>
