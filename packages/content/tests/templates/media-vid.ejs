<%- include(locals.vidTpl !== undefined ? locals.vidTpl  : '../../../medias/tests/templates/responsive-vid') %>
<% if (locals.caption != undefined) { %>
<div class="<%= prefix %>-content-media__caption" ><%= caption %></div>
<% } %>
<%
if (locals.transcription != undefined) {
let data = {
    label: transcription,
    size:'md'
}
%>
    <div class="<%= prefix %>-content-media__transcription" >
        <%- include('../../../buttons/tests/templates/button', data); %>
    </div>
<% } %>