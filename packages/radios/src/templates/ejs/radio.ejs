<%#
# paramètres

// TODO: documentation

%>
<% eval(include('../../../../core/index.ejs')); %>
<%
 let radioAttrs = locals.radioAttrs || [];
 let labelAttr = {};
 let labelClass = locals.labelClasses || [];
 locals.customIcon ? labelAttr['style'] = locals.customIcon : labelAttr = {};
 if (locals.icon) labelClass.push(prefix + '-ifi-' + locals.icon);
 if (locals.disabled) radioAttrs['disabled'] = '';
 %>

<input <%- includeAttrs(radioAttrs); %> type="radio" id="<%= id %>" name="<%= name %>">
<%- include('../../../../forms/src/templates/ejs/label', {labelClasses: labelClass, labelAttributes: labelAttr}); %>
<%- include('../../../../forms/src/templates/ejs/message'); %>
