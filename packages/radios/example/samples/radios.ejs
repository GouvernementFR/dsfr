<%
let element = {
  path: '../radios/example/samples/radio',
    data:locals.element || {}
}

element.data.name = id;

let fields = {
    choice:true,
    elements: [
        { ...element, data: { ...element.data, id: id + '-1' } },
        { ...element, data: { ...element.data, id: id + '-2' } },
        { ...element, data: { ...element.data, id: id + '-3' } },
    ]
}
%>

<%- include('../../../forms/example/samples/fieldset', fields) %>
